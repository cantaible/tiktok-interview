# 交付物C：开发复盘报告（Process Documentation）

## 1. 概述

本次开发以 **Spec-Driven Development** 为主线推进：先将需求、边界与验收标准固化为可执行的 Spec，再让 AI 在约束内生成实现。整体流程参考 spec-kit 的"Detailed Process"，按 **constitution → specification → clarification → plan → tasks → implement** 的顺序闭环迭代，并在过程中通过"改 Spec"而不是"补代码"的方式完成关键纠偏。

## 2. 工具与模型

### 开发工具
- **开发环境**: Visual Studio Code
- **AI 工具**: GitHub Copilot
- **AI 模型**: Claude Sonnet 4.5

### 开发方法
采用 spec-kit 的工作流命令体系来组织产物与迭代节奏：
- 用 **constitution** 固定工程原则
- 用 **specify** 输出需求规格
- 用 **clarify** 收敛歧义
- 用 **plan** 明确技术与架构路线
- 用 **tasks** 生成可执行任务分解
- 最后在 **implement** 阶段按任务清单实现与验证

## 3. 开发流程复盘：我如何定义 Prompt/Spec 引导 AI

### 第一阶段：建立基础规格

我没有直接让 AI 从零开始写代码，而是先提供模板化参考材料（官方文档、官方视频、第三方教程），让 AI 在统一语境下先总结 requirements，并形成初版规格草案。随后将草案收敛为 spec-kit 体系下的规格文档，确保每一条需求都能落在"可验证"的表达上，而不是停留在描述性语句。

### 第一轮迭代：快速原型

第一轮迭代中，我先定义 **constitution**，用来约束 AI 在后续阶段的输出质量与行为边界，例如：
- 不得编造不可验证的数据源
- 遇到不确定项必须先澄清或显式写明假设
- 每个功能必须对应明确的验收点
- 必须覆盖异常态与空态等

然后进入 **specification** 阶段，将题目要求整理为功能规格，尽量用用户行为、输入输出、边界条件和验收标准描述需求，而不在此阶段讨论技术实现细节。随后我按规格推动实现，快速得到可运行的雏形。

### 发现问题

但在雏形验证中，我发现部分实现"表面完成但不满足要求"，主要问题集中在：
- **数据源问题**：被 AI 用虚构或 mock 替代导致不可验证
- **日期筛选逻辑**：缺失或语义不清
- **UI 一致性**：需要进一步美化和一致性收敛

这些问题并非单纯编码失误，而是**规格表达不够刚性、验收口径不够具体**，导致 AI 在空白处选择了最低成本的补全方式。

### 第二轮迭代：精细化规格

因此第二轮迭代我新开示例分支，以更严格的"澄清—计划—任务—实现"链路推进：

1. **Clarification**: 通过澄清将歧义点前置收敛，把关键语义写成不可误解的规则
2. **Plan**: 明确技术路线、模块划分与关键约束
3. **Tasks**: 把工作拆成可执行且可验证的任务序列（必要时把测试/用例前置）
4. **Implement**: 按任务逐项实现，并把运行结果回灌到规格与任务验收点中，形成可重复的闭环

## 4. 关键修正（Crucial Fixes）：通过修改 Spec 修正 AI 生成错误

### 修正案例 1：编造数据源导致"可运行但不可验证"

**问题现象**：功能看起来齐全，但数据链路依赖虚构接口或假数据，导致运行时无法对照题目要求验证正确性。

**根因分析**：第一轮 spec 对数据来源、字段 schema、失败策略的约束不足，AI 在缺少硬约束时倾向于用 mock 数据把 UI 和流程"跑通"。

**修正方式**：
- 把数据要求提升为规格中的硬性条款
- 明确数据输入来源与形态
- 固定字段 schema（字段名、类型、可空性与示例）
- 规定加载失败、空数据的 UI 行为与提示策略
- 在验收清单中加入"数据可追溯、可复现、可对照"的检查项

**修正结果**：AI 后续生成实现时无法绕开真实数据约束，避免再次出现"用假数据完成度"冒充交付的情况。

### 修正案例 2：日期筛选遗漏或语义不一致

**问题现象**：筛选入口存在，但逻辑缺失或边界处理不一致，例如：
- 开始/结束是否包含当天？
- 空值如何处理？
- 同一天范围是否成立？
- 无结果如何呈现？

**根因分析**：spec 没有把日期筛选从"描述性需求"写成"可执行规则"，导致 AI 只能凭默认理解实现，从而产生漏项或不一致。

**修正方式**：
- 在 **clarification** 阶段把歧义点收敛为明确规则
  - 筛选语义
  - 包含关系
  - 异常输入策略
  - 空值策略等
- 把规则写入规格
- 将典型边界用例固化为验收清单（给出样例输入与期望输出）

**修正结果**：实现阶段可以按清单逐项验证，保证"功能真的实现"而不只是"界面上看起来有"。

## 5. 总结

本次实践的核心结论是：**在 spec-kit 的流程下，开发者的核心价值更像架构师而非码农**。

### 我的主要工作转变

从写实现细节转为：
- 定义原则（constitution）
- 明确需求表达（specification）
- 消除歧义（clarification）
- 制定可落地路线（plan）
- 拆解可验证任务（tasks）
- 用验收清单持续约束实现（implement）

### 关键发现

- ✅ **当规格足够清晰且验收前置时**：AI 的产出会显著更稳定、更贴合需求
- ❌ **当规格模糊时**：AI 容易用"最省事的方式"补洞，导致表面完成但不可验证

### 最终成果

通过两轮迭代与关键纠偏，我最终将产出从"能跑的雏形"推进到"可验证、可复现、符合验收口径"的交付结果。

---

**文档版本**: v1.0  
**最后更新**: 2026-01-04
