# 面试演示指南

## 🎯 项目简介

**项目名称**: Local News Harvester MVP  
**技术栈**: Next.js 14 + TypeScript + SQLite + Tailwind CSS  
**完成度**: 100% (90/90 任务)  
**演示 URL**: http://localhost:3000

---

## 📋 演示流程（5-10 分钟）

### 1️⃣ 主页 - 新闻展示 (2 分钟)

**展示内容**:
- 打开 http://localhost:3000
- 显示新闻卡片网格（已有 149 篇文章）
- 指出关键 UI 元素：
  - 响应式卡片布局（移动/平板/桌面）
  - 缩略图 + 标题 + 来源 + 时间
  - 摘要和标签
  - "Fetch News Now" 按钮
  - "Export JSON/CSV" 按钮

**说明要点**:
> "这是一个新闻聚合应用，目前展示 149 篇文章。界面采用响应式设计，支持桌面和移动端。每个新闻卡片显示标题、来源、发布时间、摘要和标签。"

### 2️⃣ 新闻抓取功能 (2 分钟)

**演示操作**:
1. 点击 "Fetch News Now" 按钮
2. 观察 Loading 状态（Toast 通知）
3. 等待 10-15 秒（抓取 9 个源）
4. 查看成功通知："Scraped XX articles, saved XX new"
5. 页面自动刷新显示新文章

**说明要点**:
> "点击抓取按钮后，后台会并发访问 9 个新闻源（包括 RSS feed 和网页）。系统会自动去重、生成摘要和标签，然后保存到数据库。整个过程约 10 秒，取决于网络速度。"

**技术亮点**:
- ✅ 并发抓取（Promise.all）
- ✅ URL + 标题相似度双重去重
- ✅ LLM 集成（可选，支持降级）
- ✅ 错误处理和源健康监控

### 3️⃣ 过滤功能 (2 分钟)

**演示操作**:
1. **日期过滤**:
   - 点击日期选择器
   - 选择特定日期
   - 观察卡片即时过滤
   
2. **来源过滤**:
   - 打开来源下拉框
   - 勾选 1-2 个来源（如 TechCrunch）
   - 查看过滤后的文章数量
   
3. **标签过滤**:
   - 在标签搜索框输入关键词（如 "AI"）
   - 点击标签云中的标签
   - 观察实时过滤
   
4. **组合过滤**:
   - 同时使用多个过滤器
   - 显示"X articles filtered"
   - 点击 "Clear All Filters" 恢复

**说明要点**:
> "过滤系统支持按日期、来源、标签的组合筛选。使用 React.useMemo 优化，确保 500 篇文章内过滤响应时间 <500ms。标签云显示最热门的 30 个标签，支持搜索。"

**技术亮点**:
- ✅ 客户端过滤（useMemo 优化）
- ✅ 多维度组合过滤
- ✅ 性能目标达成（<500ms）
- ✅ 直观的 UI 反馈

### 4️⃣ 源管理 (2 分钟)

**演示操作**:
1. 点击导航栏 "Sources" 链接
2. 查看当前 10 个新闻源列表
3. 展示源信息：
   - 显示名称和 URL
   - RSS/WEB 类型标识
   - 启用/禁用状态
   - 错误计数（如果有）
   - 最后抓取时间
   
4. **添加新源**:
   - 点击 "Add Source" 按钮
   - 选择类型（RSS）
   - 输入 URL（示例：https://example.com/feed.xml）
   - 演示自动检测 RSS 名称
   
5. **管理源**:
   - 切换启用/禁用开关
   - 查看删除确认对话框

**说明要点**:
> "源管理页面允许添加、启用/禁用和删除新闻源。RSS feeds 会自动检测名称。系统追踪每个源的健康状况，连续失败 3 次后自动禁用。"

**技术亮点**:
- ✅ CRUD 操作完整
- ✅ RSS 自动检测
- ✅ 健康监控和自动禁用
- ✅ 友好的确认对话框

### 5️⃣ 数据导出 (1 分钟)

**演示操作**:
1. 返回主页
2. 应用一些过滤器（可选）
3. 点击 "Export JSON" 按钮
4. 下载文件 `articles_2026-01-04.json`
5. 打开文件展示 JSON 结构
6. 点击 "Export CSV" 按钮
7. 展示 Excel 兼容的 CSV（UTF-8 BOM）

**说明要点**:
> "导出功能支持 JSON 和 CSV 两种格式，导出的数据会尊重当前的过滤条件。CSV 文件添加了 UTF-8 BOM，确保 Excel 能正确显示中文。"

**技术亮点**:
- ✅ 多格式支持
- ✅ 尊重过滤条件
- ✅ Excel 兼容性
- ✅ 带日期的文件名

---

## 🛠️ 技术架构讲解

### 后端技术栈
```
Next.js 14 App Router
├── API Routes (8 个端点)
│   ├── /api/articles      - GET 文章列表
│   ├── /api/scrape        - POST 抓取新闻
│   ├── /api/sources       - CRUD 源管理
│   └── /api/export        - GET 导出数据
├── SQLite 数据库
│   ├── articles 表 (10 字段 + 2 索引)
│   └── sources 表 (7 字段)
└── 业务逻辑层
    ├── RSS 解析器 (rss-parser)
    ├── 网页抓取器 (cheerio)
    ├── 去重算法 (URL + Levenshtein)
    └── LLM 集成 (Aliyun Bailian)
```

### 前端技术栈
```
React 18 + TypeScript 5.3
├── 页面组件
│   ├── / (主页 - 新闻展示)
│   └── /sources (源管理)
├── UI 组件 (15+)
│   ├── NewsCard (新闻卡片)
│   ├── FilterBar (过滤栏)
│   ├── SourceForm (源表单)
│   └── 可复用组件 (Button, Input, Badge)
└── 状态管理
    ├── useState (本地状态)
    └── useMemo (性能优化)
```

### 数据流程
```
用户点击 "Fetch News" 
  → POST /api/scrape
  → 并发抓取 9 个源
  → URL 去重
  → 标题相似度去重（85%）
  → LLM 批量丰富（可选）
  → 保存到 SQLite
  → 返回统计结果
  → 前端刷新显示
```

---

## 💡 技术亮点说明

### 1. 性能优化
- **客户端过滤**: 使用 `React.useMemo` 确保大数据集（500+ 文章）过滤 <500ms
- **批量 LLM**: 每批处理 5 篇文章，使用 `Promise.allSettled` 保证部分失败不影响整体
- **数据库索引**: `sourceName` 和 `publishedAt` 建立索引，查询速度快
- **并发抓取**: 使用 `Promise.all` 并发访问多个源

### 2. 错误处理
- **优雅降级**: LLM 不可用时使用 mock 实现（前 97 字符 + 标题提取）
- **错误边界**: React 错误边界捕获组件错误
- **HTTP 403 处理**: OpenAI Blog 防爬虫，记录错误但不中断流程
- **源健康监控**: 连续失败 3 次自动禁用源

### 3. 代码质量
- **TypeScript 严格模式**: 完整类型安全
- **ESLint 无错误**: 通过所有代码质量检查
- **模块化设计**: 清晰的职责分离（scraper/llm/db）
- **可复用组件**: 15+ UI 组件可在整个应用中使用

### 4. 用户体验
- **响应式设计**: 支持移动/平板/桌面
- **加载状态**: 骨架屏 + Toast 通知
- **即时反馈**: 所有操作都有明确的视觉反馈
- **无障碍**: 键盘导航、焦点状态、ARIA 标签

---

## 🎤 常见面试问题准备

### Q1: "为什么选择 SQLite 而不是 PostgreSQL/MySQL？"
**答**: 对于 MVP 阶段，SQLite 提供了完美的平衡：
- ✅ 零配置，单文件数据库
- ✅ 足够快（对于中等规模数据集）
- ✅ ACID 事务支持
- ✅ 易于开发和演示
- ⚠️ 扩展时可迁移到 PostgreSQL（迁移成本低）

### Q2: "客户端过滤 vs 服务端过滤，为什么选客户端？"
**答**: 
- ✅ **更好的 UX**: 即时响应（<500ms）
- ✅ **减少服务器负载**: 一次获取所有数据
- ✅ **简化 API**: 不需要复杂的查询参数解析
- ⚠️ **权衡**: 初始加载稍慢，但对于数百篇文章可接受
- 🔄 **未来优化**: 数据量大时可切换到服务端分页

### Q3: "去重算法的选择？"
**答**: 双重策略：
1. **URL 规范化**: 移除追踪参数、www、尾部斜杠
2. **标题相似度**: Levenshtein 距离，85% 阈值
   - 可处理略有差异的标题（标点、空格）
   - 防止同一新闻的不同来源重复

### Q4: "如何处理 LLM API 失败？"
**答**: 优雅降级策略：
- ✅ 检查环境变量是否配置
- ✅ API 失败时使用 mock 实现
- ✅ 批量处理使用 `Promise.allSettled`（部分失败不影响整体）
- ✅ 数据库允许 `summary` 和 `tags` 为 null
- ✅ UI 正常显示（无摘要时不显示）

### Q5: "性能瓶颈和优化空间？"
**答**: 
**当前优化**:
- ✅ useMemo 缓存过滤结果
- ✅ 数据库索引
- ✅ 批量 LLM 处理
- ✅ 并发抓取

**未来优化**:
- 🔄 虚拟滚动（react-window）处理大量卡片
- 🔄 服务端分页（数据量 >1000 时）
- 🔄 Redis 缓存热门查询
- 🔄 CDN 缓存静态资源

---

## ✅ 演示检查清单

### 演示前准备
- [ ] 确认服务器运行（http://localhost:3000）
- [ ] 数据库已初始化（149 篇文章）
- [ ] 浏览器窗口干净（无其他标签页）
- [ ] 网络连接正常（用于抓取演示）
- [ ] 终端窗口可见（显示日志）

### 演示要点
- [ ] 展示响应式设计（调整浏览器窗口）
- [ ] 演示新闻抓取（实时日志）
- [ ] 展示过滤性能（快速切换）
- [ ] 演示源管理 CRUD
- [ ] 展示导出功能
- [ ] 打开 JSON/CSV 文件展示数据

### 可能的备选问题
- [ ] 代码结构讲解（打开 VS Code）
- [ ] 数据库设计说明（展示 schema.sql）
- [ ] API 端点测试（Postman/curl）
- [ ] 错误处理演示（禁用所有源，触发错误）

---

## 🚀 快速启动命令

```bash
# 进入项目目录
cd /Users/dongzhiming/Documents/Codes/tiktok-interview

# 启动开发服务器
npm run dev

# 访问应用
open http://localhost:3000

# (可选) 重置数据库
npm run db:reset

# (可选) 查看构建
npm run build
```

---

## 📝 演示脚本（逐字稿）

### 开场白
"大家好，今天给大家演示的是 Local News Harvester MVP，一个基于 Next.js 14 和 TypeScript 的新闻聚合应用。这个项目实现了 4 个核心用户故事：新闻收集、过滤发现、源管理和数据导出。让我们从主页开始。"

### 主页介绍
"这是主页，目前展示 149 篇文章。每个卡片包含标题、来源、发布时间、摘要和标签。界面采用响应式设计，桌面端 3 列、平板 2 列、移动端 1 列。"

### 抓取演示
"现在演示新闻抓取功能。点击 'Fetch News Now'，后台会并发访问 9 个新闻源，包括 TechCrunch、The Verge、36氪等。整个过程会进行 URL 去重和标题相似度去重，然后用 LLM 生成摘要和标签。大约需要 10 秒……"

"看，抓取完成！Toast 通知显示抓取了 XX 篇文章，保存了 XX 篇新文章。页面已自动刷新。"

### 过滤演示
"接下来演示过滤功能。首先用日期过滤器选择今天的文章……可以看到卡片即时更新。再添加来源过滤，比如只看 TechCrunch 的文章……现在显示 'X articles filtered'。"

"标签过滤支持搜索，我输入 'AI'……可以看到只显示包含 AI 标签的文章。这些过滤都是客户端实现，使用 React.useMemo 优化，确保大数据集下性能 <500ms。"

"点击 'Clear All Filters' 恢复所有文章。"

### 源管理演示
"点击导航栏的 'Sources' 进入源管理页面。这里显示所有新闻源，包括类型、状态、错误计数和最后抓取时间。"

"我可以切换启用/禁用状态……删除需要确认……添加新源时，RSS feeds 会自动检测名称。"

### 导出演示
"回到主页，演示导出功能。点击 'Export JSON'……文件已下载。打开看，包含所有字段的结构化数据。CSV 导出添加了 UTF-8 BOM，确保 Excel 能正确显示中文。"

### 总结
"这就是 Local News Harvester 的核心功能演示。项目完成了 90 个任务，100% 实现了规格说明。使用 Next.js 14、TypeScript、SQLite 等现代技术栈，代码质量高，性能达标，用户体验好。谢谢大家！"

---

**演示时长**: 8-10 分钟  
**难度级别**: 中级  
**建议练习次数**: 3-5 次（确保流畅）
